# Supabase Configuration
# Get these from your Supabase project dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key-here

# Application URL
# For local development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# For production (replace with your Vercel URL)
# NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# Gemini API Configuration
# Get your free API key from: https://aistudio.google.com/app/apikey
# Using gemini-2.0-flash which is FREE with generous rate limits:
# - 15 requests per minute
# - 1,500 requests per day
# - 1 million tokens per day
GEMINI_API_KEY=your-gemini-api-key-here

# Supabase Service Role Key (Optional, for server-side API routes)
# Get this from: Supabase Dashboard → Settings → API → service_role key
# If not provided, will fallback to anon key (less secure for production)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
